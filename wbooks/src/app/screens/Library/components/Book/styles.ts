import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  book: {
    flexDirection: 'row',
  },
  textContainer: {
    width: '70%',
    marginLeft: 20
  },
  title: {
    marginBottom: 4
  }
});
